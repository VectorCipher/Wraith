"""
WRAITH Core Package

The brain of the AI penetration tester. This package contains the
autonomous components that drive WRAITH's scan lifecycle:

    Memory     — Working memory that stores everything the AI discovers
    TaskTree   — Hierarchical progress tracker for scan phases
    Orchestrator — The main think → decide → act → learn loop
"""
